pluginManagement {
    repositories {
        google()                 // MUST be first
        gradlePluginPortal()
        maven { url "https://dl.google.com/dl/android/maven2/" }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()                 // MUST be first
        maven { url "https://dl.google.com/dl/android/maven2/" }
    }
}

rootProject.name = "LGRemoteApp"
include(":app")