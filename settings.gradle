pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral() // <--- THIS WAS MISSING
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral() // <--- THIS WAS MISSING
    }
}
rootProject.name = "LGRemoteApp"
include ':app'
